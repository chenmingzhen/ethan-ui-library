{
    // 为IDEA代码提示 无实际意义
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "outDir": "/",
        // 移除严格模式 (对象可能未定义)
        "strict": false
    },
    "exclude": ["node_modules", "ssh-client", "docs-pages", "publish", "site/chunks"]
}
