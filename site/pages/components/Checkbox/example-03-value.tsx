/**
 * cn - 默认值
 *    -- 设置defaultChecked初始化值
 * en - DefaultChecked
 *    -- Set the Initialization checked
 */
import React from 'react'
import { Checkbox } from 'ethan-ui'

export default function () {
    return <Checkbox defaultChecked>defaultChecked</Checkbox>
}
